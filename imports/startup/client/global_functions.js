import { Meteor } from 'meteor/meteor';

show_document = function(fileId, target){
  // client sees name and functionality call directly no wrap meteor call
  // becasue it is in client folder and client folder is imported by main.js

  // url = meteor.call(get_url(fileId))
  // if target == 'new'
  //    window.open(url)
  // else
  //    display in existing
  //



}